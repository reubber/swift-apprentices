let item = false

var switchState = true
switchState.toggle() // switchState = false
switchState.toggle() // switchState = true

// Here, the variable called switchState starts as true. Then, after one toggle, it
// becomes false. After another toggle, it’s set to true again

var sum = 1;
var i = 0;
var y = 0;

// repeat === do while

repeat {
 sum = sum + (sum + 1)
 i+=1;
 print(sum)
 print(i)
} while sum < 1000

var sub = 1

repeat {
 sub = sub + (sub + 1)
 y+=1;
 print("sub", sub)
 print(i)
} while sub < 1

